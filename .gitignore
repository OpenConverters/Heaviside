# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
.installed.cfg

# Virtualenvs
.venv/
.venv-web/
venv/
env/
ENV/

# uv
.uv-cache/

# Tooling
.mypy_cache/
.ruff_cache/
.pytest_cache/
.pytype/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/

# Generated schema classes — never committed; regenerate with `make types`
heaviside/types/_generated/

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# Heaviside runtime artefacts
data/runtime/
data/cache/
*.ngspice.log
*.raw
results/
.heaviside/

# Secrets
.env
.env.*
!.env.example

# Locks (TAS librarian)
.locks/
*.lock
knowledge/lessons.ndjson

# Playwright MCP scratch output
.playwright-mcp/

# Generated design-report artifacts (PDF/HTML), per-run
outputs/*.pdf

# webui build deps
heaviside/webui/node_modules/

# local-only Würth REDEXPERT MCP converter tooling (not committed)
scripts/redexpert_client.py
scripts/fetch_redexpert_wurth.py

# Built web frontend (regenerate with `npm run build`; do NOT commit — it bloats history)
heaviside/api/static/

# Large catalog enrichment data artifacts (kept on disk, not in the repo)
scripts/enrichment/*.ndjson

# Report screenshots / scratch images
/evq-*.png
*.pyc

# Persisted job results (runtime, per-machine)
.heaviside/
